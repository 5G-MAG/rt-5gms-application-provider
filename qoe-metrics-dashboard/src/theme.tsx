import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#0F4776',
    },
    background: {
      paper: '#fff',
      default: '#fff',
    },
    text: {
      primary: '#000',
    },
  },
});
